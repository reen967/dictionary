# Universal Robot Dictionary Demo

This is an MVP for a human-centric "Universal Robot Language".

- Generates perceptual robot signals using a dictionary of Nouns, Verbs, and Hybrids
- Demo HTML visualizes a Sleepy → Alarmed transition
- Effort is normalized per robot
- Fully static HTML + JS using pre-generated JSON data

## Usage

1. Run `generate_demo.py` to produce `data/demo_signals.json`.
2. Open `index.html` in your browser or deploy to GitHub Pages.
